apiVersion: v1
kind: Service
metadata:
  name: hospital-app-service
spec:
  type: LoadBalancer
  selector:
    app: hospital-backend
  ports: 
    - protocol: TCP
      port: 80
      targetPort: 5000